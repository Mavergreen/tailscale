Report Mavergreen as the packaging type via Hostinfo.Package.

--- cmd/tailscaled/tailscaled.go.orig
+++ cmd/tailscaled/tailscaled.go
@@ -495,6 +495,8 @@ func run() (err error) {
 		hostinfo.SetApp(app)
 	}
 
+	hostinfo.SetPackage("Mavergreen")
+
 	return startIPNServer(context.Background(), logf, publicLogID, sys)
 }
 
